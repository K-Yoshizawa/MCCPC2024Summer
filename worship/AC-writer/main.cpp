#include <bits/stdc++.h>
using namespace std;

int main(){
    int A, B, C; cin >> A >> B >> C;
    for(int i = 0; i < A; ++i) cout << "bow" << endl;
    for(int i = 0; i < B; ++i) cout << "clap" << endl;
    for(int i = 0; i < C; ++i) cout << "bow" << endl;
}